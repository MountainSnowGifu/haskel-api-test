haskell webapi Clean Architecture 

方向性を整理しつつ、2026年時点でも通用する Haskell の Web API 構成を確認しています。主要ライブラリの現状を当たり、Clean Architecture に落とし込んだ実践形でまとめます。

主要候補は見えました。型で API 契約を先に置ける Servant が Clean Architecture と相性がよく、Scotty は薄いルーティング層として軽量ですが、境界を自分で厳密に保つ必要が強めです。

思考時間: 24s

Haskell で Web API を Clean Architecture で組むなら、まず結論はこれです。

いちばん実践的なのは Servant + WAI/Warp + effectful + PostgreSQL層 です。
Servant は API を型レベルで宣言でき、サーバ実装・クライアント生成・ドキュメント生成まで一貫して扱えるので、「外側の入出力」と「内側のユースケース」を分離する Clean Architecture とかなり相性がいいです。Servant 自体も、型安全に API 定義・サーバ・クライアント・ドキュメントを扱うことを主眼にしています。

一方で Scotty は 軽量で始めやすい ですが、README でも WAI/Warp ベースのシンプルな Web フレームワークとして位置づけられており、API 契約や境界の厳密さは Servant ほど自動では得にくいです。小規模ならよいですが、Clean Architecture を強く維持したいなら Servant のほうが素直です。

おすすめ構成
1. 依存方向

Clean Architecture では依存をこうします。

Interface/Frameworks
  ↓
Controllers / Presenters
  ↓
UseCases
  ↓
Domain

Haskell では次の対応にすると綺麗です。

Domain

純粋な型

ドメインルール

エンティティ

値オブジェクト

UseCase

入出力ポート

ユースケース

Repository の抽象

Interface

HTTP ハンドラ

JSON DTO

エラー→HTTP 変換

Infrastructure

DB 実装

外部 API 実装

ロガー

認証

Main

DI

サーバ起動

推奨ライブラリ
Web API

Servant

API を型で表現できる

サーバ実装・クライアント生成・ドキュメント生成が揃う

サーバ側は servant-server を使うのが標準的です。

HTTP 基盤

WAI / Warp

Scotty も WAI/Warp 上に乗っており、Haskell Web の基盤として広く使われます。

エフェクト管理

effectful

既存 Haskell エコシステムとの統合を強く打ち出していて、高速性も前面に出しています。

Clean Architecture では MonadFoo を大量に増やすより、UseCase が必要な効果だけを要求する 形にしやすいです。

認証

servant-auth / servant-auth-server

JWT、Basic、Cookie などを Servant の Auth combinator と一緒に扱えます。

Servant cookbook でも JWT と Basic Auth の組み合わせ例があります。

DB

Opaleye を有力候補にできます。

Postgres 向けの型安全で合成可能な SQL DSL として説明されています。

Clean Architecture 的には、UseCase からは Repository 抽象だけ見せて、Infrastructure 側で Opaleye 実装にするのが自然です。

ディレクトリ構成例
src/
  Main.hs

  Domain/
    User.hs
    Article.hs
    Error.hs

  UseCase/
    Ports/
      UserRepository.hs
      Clock.hs
      TokenService.hs
    User/
      RegisterUser.hs
      GetUser.hs

  Interface/
    Api.hs
    Dto/
      UserDto.hs
    Controller/
      UserController.hs
    Presenter/
      ErrorPresenter.hs

  Infrastructure/
    Db/
      UserRepositoryPg.hs
      Schema.hs
    Auth/
      Jwt.hs
    Logging/
      Logger.hs
    Env.hs

ポイントは、Servant の API 型を Domain に置かないことです。
API 型はあくまで外側の表現なので Interface/Api.hs に置くのが無難です。

役割分担の具体像
Domain
newtype UserId = UserId UUID
newtype Email = Email Text

data User = User
  { userId :: UserId
  , email  :: Email
  }

ここには ToJSON, FromJSON, Servant, Opaleye 依存をなるべく入れない

純粋なビジネス概念だけ置く

UseCase
class Monad m => UserRepository m where
  findUserById :: UserId -> m (Maybe User)
  insertUser   :: Email -> m User

getUser :: UserRepository m => UserId -> m (Either AppError User)
getUser uid = ...

ユースケースは HTTP を知らない

DB も知らない

「何をしたいか」だけを書く

Interface
type UserAPI =
       "users" :> Capture "id" UUID :> Get '[JSON] UserResponse
  :<|> "users" :> ReqBody '[JSON] CreateUserRequest :> Post '[JSON] UserResponse

HTTP の都合をここに閉じ込める

JSON DTO と Domain 型の変換をここで行う

Infrastructure
runUserRepositoryPg :: Pool Connection -> ...

Repository の具象実装

JWT 発行

Logger

設定ファイル読み込み

Servant を選ぶ理由

Servant は API 契約を型として先に置けるので、Clean Architecture でいう「外部との境界」が明確になります。
さらに Servant は、同じ API 定義からサーバ実装やクライアント生成、ドキュメント生成に展開できる設計思想を持っています。これが「境界の一元管理」と相性がいいです。

感覚的にはこうです。

Scotty: ルートを書きながらアプリを作る

Servant: 契約を書いてから実装を埋める

Clean Architecture は後者の流れに乗せやすいです。

effectful を使う理由

Haskell では ReaderT Env IO も定番ですが、規模が大きくなると

DB

Logger

Clock

Auth

Transaction

External API

のような関心が増えます。

effectful は高速性と既存ライブラリとの統合を前面に出していて、ユースケースから必要な能力だけ要求する形にしやすいです。
そのため、

Domain は純粋

UseCase は抽象的な効果

Infrastructure で実行

という分離がしやすいです。

最小の設計パターン

実際には次の 3 つだけ守るとかなり崩れません。

1. Domain は純粋にする

aeson

servant

opaleye

persistent

を Domain まで入れない

2. UseCase は HTTP を知らない

たとえば 404 や 401 を直接返さず、AppError を返す。

data AppError
  = UserNotFound
  | InvalidEmail
  | Unauthorized

HTTP への変換は Interface 側。

3. Infrastructure は抽象を実装するだけ

DB 実装や JWT 実装は UseCase が定義したポートに従わせる。

典型的なリクエストの流れ
HTTP Request
  -> Servant Handler
  -> DTO decode
  -> UseCase 呼び出し
  -> Repository / Auth / Clock などのポート使用
  -> Domain result / AppError
  -> Presenter で HTTP Response 化

これで「外の表現」と「中のルール」が混ざりにくくなります。

ありがちな失敗
1. Domain に JSON/DB インスタンスを全部載せる

最初は楽ですが、あとで層が溶けます。

2. Servant Handler の中で SQL を書く

これは Clean Architecture 的にはかなり崩れやすいです。

3. Env をどこでも触れる万能箱にする

依存が見えなくなります。
UseCase には必要な能力だけ渡すほうが保守しやすいです。

4. DTO と Domain を同一化する

外部公開 API の都合で Domain が歪みます。

2026年時点での現実的な選択

私ならこうします。

堅めの本命

servant-server

wai

warp

effectful

servant-auth-server

PostgreSQL

opaleye

軽量に始める

scotty

wai

warp

ReaderT Env IO

ただし後者は、長く育てる API だと境界管理を自分でかなり厳格にやる必要があります。
Scotty は軽量さが魅力ですが、Servant のような API 契約中心の恩恵は薄いです。

まず作るならこの順番

Domain にエンティティとエラーを書く

UseCase にポートを書く

UseCase に業務ロジックを書く

Interface に Servant API と DTO を書く

Infrastructure に DB/JWT 実装を書く

Main で全部組み立てる

この順序にすると、Web フレームワークに引っ張られずに済みます。

ひとことで言うと

Haskell で Clean Architecture をやるなら、Servant を「外側の契約」、UseCase を「中心」、effectful を「依存の接着剤」として使うのがかなり強いです。
Servant は API 契約の型安全さとドキュメント・クライアント生成の一貫性が強みで、effectful はユースケースの依存を整理しやすく、認証は servant-auth 系、DB は Postgres 向けに Opaleye のような型安全 DSL が有力です。

必要なら次に、**「Servant + effectful で作る最小 Web API の雛形」**をそのまま cabal/stack で動く形で出します。