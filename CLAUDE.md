## Claude への指示

- コードの説明は**型から始める**（型シグネチャを先に示してから実装を説明）
- エラーが出た場合は GHC のエラーメッセージを貼ること
- 「なぜそう動くのか」の原理を重視し、単なる答えより**概念の理解**を優先する
- サンプルコードは必ず `ghci` で動作確認できる形で書く
- モナドの説明では「箱」「文脈」などの比喩を使いすぎず、**型で考える**習慣をつける
